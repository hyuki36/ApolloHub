name: ApolloHub review bot

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apollohub-bot-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch approved scripts from live site
        env:
          BOT_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sL -X POST "https://apollohub-nu.vercel.app/api/bot/export" \
            -H "Authorization: Bearer $BOT_TOKEN" \
            -o /tmp/apollo_export.json
          python3 bot/sync.py

      - name: Commit if changed
        run: |
          git config user.name "apollohub-bot"
          git config user.email "apollohub-bot@users.noreply.github.com"
          git add scripts.json
          git diff --cached --quiet && echo "nothing new" && exit 0
          git commit -m "Bot: approve scripts"
          git push
